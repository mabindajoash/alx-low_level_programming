#include "main.h"
#include <math.h>
#include <stdio.h>

/**
 * _sqrt_recursion - returns the natural squareroot of a number
 * @n: given number
 * Return: natural squareroot of a number
 */

int _sqrt_recursion(int n)
{
	int res;

	res = sqrt(n);
	return (res);
}
