#include <stdio.h>

/**
  * main - entry point takes no argument
  *Return: Value is 0
  */
int main(void)
{
	char (n);

	for (i = 'a'; i < = 'z'; i++)
	{
		putchar(n);
	}
	putchar("\n");
	return (0);
}
