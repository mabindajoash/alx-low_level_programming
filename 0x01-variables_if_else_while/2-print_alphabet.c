#include <stdio.h>

/**
  * main - entry point takes no argument
  *Return: Value is 0
  */
int main(void)
{
	int (i);

	for (i = 'a'; i < = 'z'; i++)
	{
		putchar(i);
	}
	putchar("\n");
	return (0);
}
